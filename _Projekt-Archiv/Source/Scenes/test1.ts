namespace Revived {
    export async function test(): ƒS.SceneReturn {
    // await ƒS.Character.animate(characters.bonny, characters.bonny.pose.sleepy, bonnyIntro()); 
    // ƒS.Character.hideAll();
    // await ƒS.Character.animate(characters.bonny, characters.bonny.pose.irritated, testAnimation()); 
    // ƒS.Character.hideAll();


    }
}